#include <bits/stdc++.h>

using namespace std;

//even than not second possible odd than deleting and keep max freq


int main() {
    int t;
    cin>>t;
    while (t--)
    {
    long long int n;
    cin>>n;
    string s;
    cin>>s;
    s="0"+s+"0";
    const long long int mx=26;
    
    if(n%2==0)
    {
        vi oddcnt(mx),evencnt(mx)
        long long int o=0,e=0;
        for (long long int i = 0; i < n; i++)
        {
            /* code */
        }
        
    }
    }
    

    return 0;
}
